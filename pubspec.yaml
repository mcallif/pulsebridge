name: pulsebridge
description: Voice + Motion → BPM
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^5.0.0
  just_audio: ^0.9.38

flutter:
  uses-material-design: true
  assets:
    - assets/pulse.wav
